require "spec_helper"

describe CollegeAdmin do
  it "has a version number" do
    expect(CollegeAdmin::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
